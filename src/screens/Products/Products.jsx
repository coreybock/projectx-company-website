import React from "react";

import * as s from "./Products.style";

const Products = (props) => {
    return (
        <s.ProductsContainer>

        </s.ProductsContainer>
    );
};

export default Products;
