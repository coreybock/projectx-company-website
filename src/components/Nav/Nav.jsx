import React from "react";

import * as s from "./Nav.style";

const Nav = (props) => {
    return (
        <div className="Nav-Container">
            I am Nav
        </div>
    );
};

export default Nav;
